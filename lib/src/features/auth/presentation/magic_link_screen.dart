import 'package:flutter/material.dart';

class MagicLinkScreen extends StatelessWidget {
  const MagicLinkScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Magic Link Screen'),
    );
  }
}
